// This is sarcastic.js,
// a JavaScript library
// designed for human readability.
// Talk normally through code.


var isScrewed = true;

String.prototype.yell = function() {
	return this.toUpperCase();
}

String.prototype.whisper = function() {
	return this.toLowerCase();
}

String.prototype.wowYourFriends = function() {
	// blinking text
	// use timeouts to control element's display property
}

function blinkingText(obj) {
	// accept a jQuery object as param (if none, use document)
	// get text in param
	// for each text, do .wowYourFriends()
}